# Sanity CMS — public, safe to expose
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Supabase — server-only (RSVP API route only, no NEXT_PUBLIC_ prefix)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Resend — server-only, never NEXT_PUBLIC_ (stubbed until Sprint 3)
RESEND_API_KEY=

# Site URL — public
NEXT_PUBLIC_SITE_URL=
